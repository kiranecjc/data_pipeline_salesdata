This project creates an ETL pipeline. The data used is the historical sales data.
pySpark is used for data handling. A sample analytics is performed to show that
the data amount is reduced and the reduction is relatively error free.
Random forest method of regression is used for analytics.
"Item count per month" is used as the value to predict for regression.